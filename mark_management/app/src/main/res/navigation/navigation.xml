<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
            xmlns:app="http://schemas.android.com/apk/res-auto"
            xmlns:tools="http://schemas.android.com/tools"
            android:id="@+id/navigation"
            app:startDestination="@id/mainFragment">

    <fragment
            android:id="@+id/mainFragment"
            android:name="com.example.a4_basic.MainFragment"
            android:label="fragment_main"
            tools:layout="@layout/fragment_main">
        <action
                android:id="@+id/action_mainFrag_to_addFrag"
                app:destination="@id/addFragment"/>
        <action
                android:id="@+id/action_mainFrag_to_editFrag"
                app:destination="@id/editFragment"/>
    </fragment>
    <fragment
            android:id="@+id/addFragment"
            android:name="com.example.a4_basic.AddFragment"
            android:label="fragment_add"
            tools:layout="@layout/fragment_add">
        <action
                android:id="@+id/action_addFrag_to_mainFrag"
                app:destination="@id/mainFragment"/>
        <action
                android:id="@+id/action_add_pop"
                app:popUpToInclusive="true"
                app:popUpTo="@id/addFragment"/>
    </fragment>
    <fragment
            android:id="@+id/editFragment"
            android:name="com.example.a4_basic.EditFragment"
            android:label="fragment_edit"
            tools:layout="@layout/fragment_edit">
        <action
                android:id="@+id/action_editFrag_to_mainFrag"
                app:destination="@id/mainFragment"/>
        <action
                android:id="@+id/action_edit_pop"
                app:popUpToInclusive="true"
                app:popUpTo="@id/editFragment"/>
    </fragment>
</navigation>